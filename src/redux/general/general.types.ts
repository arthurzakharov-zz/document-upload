export type GeneralReducerType = {
  isLoading: boolean;
  caseReference: string;
  token: string;
};

export type SetCaseReferenceAndTokenPayloadActionType = {
  caseReference: string;
  token: string;
};
